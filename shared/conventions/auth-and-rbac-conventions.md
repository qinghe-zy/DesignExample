# 认证与 RBAC 规范

## 当前基线

当前仓库默认采用：

- JWT 认证
- 角色权限控制（RBAC）
- 菜单控制路由
- 预留操作级权限扩展点

## 最小认证流程

1. 用户提交用户名和密码
2. 后端校验身份
3. 后端签发 JWT
4. 前端保存 token 并获取当前用户信息
5. 受保护接口通过 Bearer Token 校验

## 角色与菜单模型

当前建议具备：

- 用户拥有角色
- 角色承载权限意义
- 菜单记录可驱动侧边栏和权限路由

## 后续扩展方向

- Refresh Token
- 多角色聚合
- 数据权限
- 部门权限
- 租户隔离
