# Projects

`projects/` contains the current runnable baseline project plus fixed initial seed projects for this run.

## Current Project Types

- `00-base-admin/`: the current runnable reference implementation
- seed projects such as `student-management/`, `library-management/`, and `exam-system/`: consistent inheritance scaffolds for future implementation

The current set of project folders is a starting collection for this run, not the final permanent list for the factory as a whole.
